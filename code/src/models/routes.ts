export enum Routes {
  HOME = '/',
  IMAGE = '/image',
  CREATE_IMAGE = `/image/create`,
  RETREIVE_IMAGE = `/image/get/:id`,
}

export enum ComfyUIEndpoints {
  PROMPT = 'prompt',
  QUEUE = 'queue',
}
