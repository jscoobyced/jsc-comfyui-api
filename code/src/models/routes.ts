export enum Routes {
  HOME = '/',
  IMAGE = '/image',
  IMAGE_CREATE = `/image/create`,
  IMAGE_GET = `/image/get/:id`,
}

export enum ComfyUIEndpoints {
  PROMPT = 'prompt',
  QUEUE = 'queue',
  HISTORY = 'history',
}
